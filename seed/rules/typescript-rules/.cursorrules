# TypeScript Rules

- 所有函数必须有明确的返回类型注解
- 禁止使用 `any`，用 `unknown` 替代
- 优先使用 `interface` 而非 `type`（对象类型）
- 使用 `const` 优先，避免 `let`
- 异步函数统一使用 `async/await`，不用 `.then()` 链
- 导入顺序：node 内置 → 第三方 → 本地，每组之间空一行
- 文件名使用 kebab-case
- 组件/类使用 PascalCase，函数/变量使用 camelCase
